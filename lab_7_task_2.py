import numpy as np
import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation

fig, ax = plt.subplots()
pip= plt.plot([], [], 'o', color='r', label='pip')

    alpha = np.arange(-2*np.pi, 2*np.pi, 0.1)
    x = R*np.cos(t)
    y = R*sin(t)
    return x, y 



